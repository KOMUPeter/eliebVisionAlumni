{% extends 'base.html.twig' %}

{% block title %}Balance{% endblock %}

{% block body %}
    <h2>Balance</h2>
    <p><a class="nav-link" href="{{ path('myAccount') }}">My Account</a></p>
    <p>Total Registration Amount: {{ balance.totalRegistrations }}</p>
    <p>Total Payments Amount Made: {{ balance.totalMonthlyPayments }}</p>
    <p>Total Events Cost: {{ balance.totalEventsCost }}</p>
    <p>Balance: {{ balance.balance }}</p>
{% endblock %}
